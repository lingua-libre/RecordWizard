'use strict';

( function ( mw, rw ) {
	/**

		 /* Methods */
		 methods: {

		 }
	 } );
	* The Navigation vue
	*/
	rw.vue.navigation = new Vue( {
		/* Data */
		data: {
			state: rw.store.state.data,
		},

}( mediaWiki, mediaWiki.recordWizard ) );
